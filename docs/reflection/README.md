# Self-Reflection Log

A chronological record of aaOS reading its own code, finding bugs, proposing features, and having those results reviewed and sometimes shipped. Each entry is verified against git commits and observed behavior; where a number was estimated at the time and later corrected, both the original and the correction are recorded.

Each run or prep entry lives in its own file, dated `YYYY-MM-DD-<slug>.md`. New entries are added as new files, not appended to a monolith. For the build history that preceded this log, see [`../retrospective.md`](../retrospective.md). For cross-cutting lessons, see [`../patterns.md`](../patterns.md).

On cost figures: see [`cost-bookkeeping.md`](cost-bookkeeping.md) for the authoritative framing (dashboard > token math) and the running cumulative total.

## Entries

Chronological, oldest first:

- [`2026-04-13-run-1-security-self-audit.md`](2026-04-13-run-1-security-self-audit.md) — Run 1: first time the runtime read its own source; found 4 real vulnerabilities including a Phase-A path traversal bug. Integration `82d19e9`.
- [`2026-04-13-run-2-capability-revocation.md`](2026-04-13-run-2-capability-revocation.md) — Run 2: system proposed and drafted capability revocation; shipped as `f1732d9`.
- [`2026-04-13-run-3-constraint-enforcement.md`](2026-04-13-run-3-constraint-enforcement.md) — Run 3: `max_invocations` was decorative; the system noticed and we shipped enforcement in `f106d97`.
- [`2026-04-13-interlude-skill-loading-bug.md`](2026-04-13-interlude-skill-loading-bug.md) — Skill-loading bug observed in runs 2-3: Bootstrap was naming children after skills without calling `skill_read`. Manifest-only fix in `66542bf`.
- [`2026-04-14-run-4-meta-cognitive-coordinator.md`](2026-04-14-run-4-meta-cognitive-coordinator.md) — Run 4: first skill-driven run; proposed a Meta-Cognitive Coordinator. Shipped as a minimal version (`file_list` + stable Bootstrap ID + opt-in persistent memory + manifest protocol) after peer review.
- [`2026-04-14-run-5-first-persistent-memory.md`](2026-04-14-run-5-first-persistent-memory.md) — Run 5: first end-to-end persistent-memory run. Three manifest-only tuning fixes; exposed child-memory-orphaning and the JS+Python over-build.
- [`2026-04-14-run-6-kernel-gated-handoff.md`](2026-04-14-run-6-kernel-gated-handoff.md) — Run 6: two kernel-level gaps surfaced and fixed — stable-identity gate on private memory (`505f559`) and structured `prior_findings` handoff (`5feedbe`).
- [`2026-04-14-run-7-kernel-fixes-validated.md`](2026-04-14-run-7-kernel-fixes-validated.md) — Run 7/7b: validated Run 6's kernel fixes under live traffic; no new code shipped. Process lessons on `--no-cache` builds and workspace export before `docker rm`.
- [`2026-04-14-run-7-followup-error-handling.md`](2026-04-14-run-7-followup-error-handling.md) — Run 7 follow-up: acted minimally on the error-handling proposal. `MemoryResult2` renamed; `ContextSummarizationFailed` audit event actually fires now (`ba0904a`, `51db7b5`).
- [`2026-04-14-run-7-followup-phase1-speed.md`](2026-04-14-run-7-followup-phase1-speed.md) — Run 7 follow-up: Phase 1 speed work — `file_read_many` batch tool, chain trim, output scoping. Shipped as `5be74ac`.
- [`2026-04-14-run-8-phase1-measured.md`](2026-04-14-run-8-phase1-measured.md) — Run 8: measured Phase 1 speed work — ~50% reduction, beating the 35-45% target. Peer-review emergence pattern first observed.
- [`2026-04-14-run-9-adversarial-bug-hunt.md`](2026-04-14-run-9-adversarial-bug-hunt.md) — Run 9: adversarial bug-hunt prompt found seven real bugs including the symlink bypass of the Phase-A path-traversal fix. Eight commits, five Copilot-pushback revisions.
- [`2026-04-14-run-10-persistent-memory.md`](2026-04-14-run-10-persistent-memory.md) — Run 10: persistent memory carried forward from Run 9; found a `spawn_with_tokens` gap in Run 9's Fix 1. Crossed $1.00 cumulative dashboard spend across ten runs.
- [`2026-04-14-run-11-prep-docs-masking-parallelism.md`](2026-04-14-run-11-prep-docs-masking-parallelism.md) — Run 11 prep: docs masking at container launch + `spawn_agents` batch tool (three Copilot review rounds, best-effort semantics). Commits `73b3653`, `04dc0c7`.
- [`2026-04-15-namespaced-backend-and-droplet-prep.md`](2026-04-15-namespaced-backend-and-droplet-prep.md) — Handle-token migration (`14a8eae`/`18d14f0`/`3c82f6e`) and namespaced-backend scaffolding + finish (`a84cd98`/`a73e062`/`8a70a1a` scaffold, `1d6ec97`/`67c7fc3` kernel launch mechanics). Four Copilot rounds on the plan; isolated cloud dev VM for the clone+exec bring-up. End-to-end confirmed on Debian 13 / kernel 6.12.43: `Seccomp: 2` + `NoNewPrivs: 1` in `/proc/<pid>/status`; 4 integration tests green under `--ignored`.
- [`2026-04-15-phase-f-a-deb-package.md`](2026-04-15-phase-f-a-deb-package.md) — Phase F-a shipped: `agentd` as a Debian `.deb`, built via `cargo-deb` (metadata on the `agentd` crate, no hand-maintained `debian/` tree). One Copilot review round caught six substantive items (socket path under `RuntimeDirectory=`, dropped `curl/jq` deps, `ProtectSystem=full` not `strict`, `postinst` → `StateDirectory=` only, `postrm` no silent errors, Debian 13 build container). Release-build breakage surfaced: `CapabilityRegistry::inspect` was `cfg(debug_assertions)`-only and two production callers depended on it — fixed with `token_id_of()`. Commits `5717906` + `8d45691`. End-to-end verified on a Debian 13 cloud VM: install → start → socket serves JSON-RPC → purge cleans state + user.
- [`2026-04-16-agentd-cli-shipped.md`](2026-04-16-agentd-cli-shipped.md) — Operator CLI shipped: five subcommands (`submit`, `list`, `status`, `stop`, `logs`) + server-side NDJSON streaming (`agent.submit_streaming`, `agent.logs_streaming`) + `BroadcastAuditLog` + explicit `aaos` system group + `agentd(1)` man page. 18-task plan, subagent-driven implementation. End-to-end verified on a Debian 13 cloud VM with a non-root operator in the `aaos` group running two real DeepSeek-backed goals (first spawned Bootstrap in 5s, second reused it in 3s). Caught a socket-permissions bug that only surfaces under non-root group members — `UnixListener::bind` inherits the process umask, so an explicit `chmod 0660` is needed after bind. Commits `58dd1bb` through `5e01acc`.
- [`2026-04-16-computed-orchestration-shipped.md`](2026-04-16-computed-orchestration-shipped.md) — Computed orchestration shipped: two-phase boot replacing Bootstrap-as-LLM-orchestrator. A cheap-LLM Planner emits a structured JSON Plan; a deterministic Rust `PlanExecutor` walks the subtask DAG via role-based scaffold spawns, running independent subtasks in parallel batches via `futures::try_join_all`. 17 commits (`9b001cb` through `cbd3dc7`), 126 new unit tests, subagent-driven with model-per-task-complexity (haiku/sonnet/opus). End-to-end verified with a real DeepSeek submit of "fetch HN and lobste.rs, compare top 3, write to /data/compare.md" — planner produced the expected 5-subtask DAG with 2 parallel fetchers. Three planner-prompt bugs surfaced (workspace-as-directory, operator-path routed through `{run}`, over-decomposition); none require runtime changes, all are next-commit prompt-tuning work. Architectural claim verified: orchestration is now code, not improvisation.
- [`2026-04-17-role-budget-wiring.md`](2026-04-17-role-budget-wiring.md) — Four benchmark runs across the day walked the canonical HN+lobste.rs goal from 5m30s (baseline) to **28s** (12× improvement). Root cause of the fetcher stall traced via Explore subagent: `Role::render_manifest` silently dropped `budget`; `execute_agent_for_subtask` used `ExecutorConfig::default()`. Fixed with `SubtaskExecutorOverrides` threading + tightened role prompts (`ef45e61`, `c412a14`). Writer's "ERROR: missing input <path>" contract fired correctly — the silent-quality-failure mode is closed. New failure mode surfaced: fetcher LLM emits plausible-path acks **without calling file_write**, proving prompt-only contracts can't enforce tool-call side effects. Next: deterministic fetcher scaffold skipping the LLM entirely for mechanical I/O.
- [`2026-04-17-replan-on-subtask-failure.md`](2026-04-17-replan-on-subtask-failure.md) — First observe→replan primitive. `PlanExecutor::execute_plan` now drains the batch with `join_all`, audits `SubtaskCompleted{success:true|false}` per subtask, and returns `Correctable("subtask '<id>' (role '<role>') failed: <err>")` on the first failure — the outer `run()` loop replans with that context. Replan prompt tightened to instruct diagnosis and non-repetition. Verified in Docker: failure-dominated goal bounds cleanly at `max_replans=3` (26s); recoverable goal pivots the fetcher URL on the first replan and completes in 82s with real HN content. Commit `54cf501`; 4 new executor tests, 138 runtime tests green.
- [`2026-04-17-namespaced-re-verification.md`](2026-04-17-namespaced-re-verification.md) — Re-verified `NamespacedBackend` on a fresh DO droplet (Debian 13, kernel 6.12.43) against current `main` (`3e1b207`). All 4 `--ignored` integration tests green; live worker's `/proc/<pid>/status` shows `NoNewPrivs:1 / Seccomp:2 / Seccomp_filters:2`. No regression across the ~20 commits since the 2026-04-15 baseline. Fixed stale `architecture.md` drift that still said "not yet functional." Phase F-b now unblocked.
- [`2026-04-17-secret-isolation.md`](2026-04-17-secret-isolation.md) — Close API-key exfiltration paths before F-b. Three changes: `/etc/default/aaos` tightened to `0600 root:root` (postinst enforces on upgrade), `agentd` scrubs the key from its own process environment at startup (libc `getenv` + byte-zero + `remove_var` — closes `/proc/<pid>/environ` read), man page documents the contract. Verified end-to-end in a fresh Docker container: `aaos`-group members can no longer read the env file; `sk-...` bytes no longer present in the daemon's `/proc/environ`; daemon still calls DeepSeek successfully. Commit `37590a0`+pending.
- [`2026-04-17-first-self-build-attempt.md`](2026-04-17-first-self-build-attempt.md) — First self-build attempt on a DO droplet: `cargo_run` tool + `builder` role let the agent read a markdown plan, read source, run `cargo check` + `cargo test` against aaOS from inside aaOS, and write a report. Four runs total; three exposed setup bugs (glob syntax `/**` vs `*`, Planner rewrites operator paths without the "operator-stated" keyword, default iteration budget of 11 is too small for builder work). Run 4 reached the real work and correctly concluded the plan had already been implemented — an honest "already done" with zero fabricated edits. Also surfaced a real test-hygiene bug: `submit_streaming_writes_events_then_end_frame` fails on hosts where `/etc/aaos/roles/` exists because `load_role_catalog` reads it directly without test isolation. Commits `45ce06b` + `755149e`.
- [`2026-04-17-self-build-tool-gap.md`](2026-04-17-self-build-tool-gap.md) — Runs 5 and 6 attempted the test-hygiene fix surfaced by run 4. Both failed to produce a diff, not from poor reasoning but from tool gaps: `file_read` dumps the whole file (no offset/limit), and there is no `file_edit` primitive — `file_write` rewrites the entire file, which for a 100KB source file with a 3-line change blows the 8KB output budget. Run 5 looped on re-reading server.rs trying to find a line number in a wall of text; run 6 fell silent trying to emit a 25k-token `file_write`. Fix applied by hand (`AAOS_ROLES_DIR` → nonexistent path + `ROLES_DIR_LOCK` mutex for env-var races). Three follow-ups queued: offset/limit on `file_read`, a new `file_edit` tool, and a recognition that the role's output budget is the wrong lever.
- [`2026-04-17-aaos-edits-aaos.md`](2026-04-17-aaos-edits-aaos.md) — First end-to-end self-build success. With `file_edit` + `file_read(offset, limit)` shipped in `2819921`, run 7 read the test-hygiene plan, paged through a 2700-line Rust source file with 9 `file_read(offset, limit)` calls, made 5 `file_edit` calls (all accepted on first try, no ambiguity errors), ran `cargo check` and `cargo test --lib submit_streaming` — both passed — and wrote an honest report. 471 s wall clock on a 4 vCPU droplet. The agent's diff is **byte-identical** to the manual fix at `f6dbc97`. Runs 5 and 6 failed on the same plan with the same LLM; the only difference was the tool set. Empirically confirms the prior lesson: self-build is tool-bound, not model-bound.
- [`2026-04-17-junior-senior-grep-tool.md`](2026-04-17-junior-senior-grep-tool.md) — First feature developed end-to-end by aaOS itself under a junior-senior workflow. Run 8 (414 s, 25 tool calls): junior implemented a new `grep` tool — new source file (~160 LoC + 3 tests), `lib.rs` exports, 3 tool-registry registrations in `server.rs`. The first `file_edit` for the registrations hit the "old_string matches 3 times" refusal; junior recovered by shortening the anchor to a unique single line and passing `replace_all: true`. Shipped as `7004b78` (was `677a58f` pre-rewrite). Run 9 (254 s, 24 tool calls, zero errors) closed the five gaps senior review flagged to make it prod-ready: declared ripgrep as a `.deb` runtime dep, granted `"tool: grep"` to the builder role, fixed doc drift (README tool count + table + architecture.md), and gated the 3 grep tests behind `#[ignore]` so a fresh machine without ripgrep doesn't flap. Shipped as `533873e` (was `71ce3ec` pre-rewrite). Commit trailers record aaOS as co-author. The junior-senior division (senior writes plans + reviews; junior writes code) is now the working loop; the next tool expansion is narrow `git_commit` so the junior can close its own loop into version control.
- [`2026-04-17-grep-cleanup.md`](2026-04-17-grep-cleanup.md) — Run 10 (169 s, 9 tool calls, zero errors — fastest self-build to date). Closed the two known issues in the grep tool: switched ripgrep invocation to `--json` output so the parser stops depending on `:` as a delimiter (fixes filenames with embedded colons), and dropped the misleadingly-named `--max-count` flag which was per-file rather than global. Added a regression test. Shipped as `62f2afa`.
- [`2026-04-17-tool-repeat-guard.md`](2026-04-17-tool-repeat-guard.md) — Run 11 (272 s, ~35 tool calls) shipped the **tool-repeat guard**: `ToolInvocation` now tracks `(agent_id, tool, input_hash) → attempt_count` and injects a `_repeat_guard` hint into successful tool results once the same call has fired 3+ times in a subtask. Closes the looping failure mode from runs 3/5/6 where the LLM re-called the same tool with the same args without noticing. Also the first run to run out of iteration budget mid-implementation — junior correctly landed the feature's core logic (6 edits, all compile, all prior tests green) but left 4 unit tests unwritten; senior handoff finished them in ~10 minutes. Led to a follow-up YAML tune: `builder.retry.max_attempts` 30 → 60 (iteration ceiling 40 → 70), plus a "plan-complete checklist" in the system prompt telling the agent to run verification + write the report before responding. Shipped as `19b2104` + follow-up. Run 11 also produced the richest self-build trace yet: 8 `grep` calls for exhaustive-match-site discovery — first real workout of the navigation primitive added in run 8.
- [`2026-04-17-git-commit-tool.md`](2026-04-17-git-commit-tool.md) — Run 12 (272 s, 45 tool calls) shipped the **`git_commit` tool**: new `Capability::GitCommit { workspace }` variant, `"git_commit: {path}"` grant-syntax parser in two sites, ~200-LoC Tool impl with subcommand allowlist `{add, commit}`, message-starts-with-dash rejection, 60s timeout, commit-SHA return. Registered in all three server tool-registry sites; builder role granted. Four new unit tests (1 capability + 3 tool-level, 3 `#[ignore]`-gated). First run under the new 60-iteration budget + plan-complete checklist from run 11: budget helped (45 calls vs run 11's 35 — ~25% more work), but the checklist didn't fire — agent said "complete" with architecture.md edit + report still unwritten. Senior touch-ups: architecture.md completion + one stale docstring. Shipped as `ad87de6`. Fifth primitive in the coding-agent surface (`file_read(offset, limit)`, `file_edit`, `file_list`, `grep`, `git_commit`) — matches the Claude Code / Cursor working subset.
- **CI pipeline landed *(2026-04-17)*** — First GitHub Actions workflow at `.github/workflows/ci.yml`: three jobs on Ubuntu 24.04 runners. `check-lint` runs `cargo fmt --check` (strict), `cargo clippy` (advisory — ~35 pre-existing warnings queued for cleanup), and gitleaks scanning. `test` runs the 499-test default suite. `test-ignored` installs ripgrep + git and runs the 15 `#[ignore]`-gated tests that need host tools or Linux kernel primitives (unprivileged user namespaces, Landlock, seccomp — all available on Ubuntu 24.04 runners, matching what the namespaced backend was verified against on Debian 13 / kernel 6.12.43). Pre-CI auto-format normalized 456 rustfmt diffs across 14 files — no behavior change, just whitespace the fmt gate now enforces. Follow-up work: clean up clippy warnings and flip the advisory gate to strict.
- [`2026-04-18-broker-worker-stream.md`](2026-04-18-broker-worker-stream.md) — Persistent broker↔worker stream with request/response correlation shipped (`7f7894d`); worker-side forgery threat class upgraded from **CLOSED BY DESIGN, WIRING PARTIAL** to **CLOSED, TRANSPORT EXERCISED**. Added `Request::Ping`, two new `#[ignore]`-gated integration tests (`ping_roundtrips_over_persistent_stream`, `worker_cannot_execve` upgraded from scaffold). First droplet run surfaced a latent seccomp bug that had been silently killing every namespaced worker right after `sandboxed-ready` for six+ weeks: mio 1.1 uses `epoll_wait` and `epoll_pwait2`, neither in the allowlist. Previous sandbox verifications measured `Seccomp: 2 / NoNewPrivs: 1` correctly but never issued a post-handshake syscall that would have tripped the missing entries; the `/dev/null` stderr-redirect in `child_fn` hid the tokio panic entirely. Fixed in `f284cc0`. Lesson worth lifting: "cosmetic error output" claims are conjectures until a test exercises the path they hide.
- [`2026-04-17-hardening-pass.md`](2026-04-17-hardening-pass.md) — Self-directed audit + fix pass after run 12. Five commits: executor-enforced single-path output contract (a role with `file_write: {param}` no-wildcard grants must produce that file — backstops the prompt-only "plan-complete checklist" that didn't fire in run 12); client-disconnect aborts the spawned executor task (no more orphan builder agents after submit tear-down); `web_fetch` streams chunks to the `max_bytes` cap instead of buffering the full body (closes an OOM risk on large responses); bounded `repeat_counts` map at 1024 entries with bulk quarter-eviction; deprecation + doc papercuts. 490 tests pass. Also surfaced a calibration lesson: an unconstrained "find bugs" audit generated 9 candidates, only 3 of which survived code-level verification.
- [`2026-04-18-mcp-integration.md`](2026-04-18-mcp-integration.md) — Bidirectional MCP integration shipped behind `--features mcp` (new `crates/aaos-mcp`). Subagent-driven, 14 tasks + two-stage review (spec compliance then code quality) per task. Reviewers caught real bugs the implementer missed: reconnect loop never wired, transport not swapped on reconnect, `u64` id type breaking MCP clients that send string ids, malformed-body returning HTTP 422 instead of JSON-RPC parse error, test race between `tokio::spawn` and the first request, vacuous `is_null` assertion, echo-server build path not respecting `CARGO_TARGET_DIR`. 24 commits on `main`. Droplet e2e validated the full control flow: `tools/call` for `submit_goal` → bootstrap spawns → DeepSeek call → tool invocation → capability denial all correctly observed.
- [`2026-04-18-f-b1-e2e-qa.md`](2026-04-18-f-b1-e2e-qa.md) — First on-production exercise of Phase F-b sub-project 1 (scheduler + TTL, 18 commits `c2b56de`..`d90189d`) on a fresh DO droplet. Baseline computed-orchestration, MCP SSE stream, and wall-clock-TTL-kill behavior all work. Seven bugs surfaced that survived two-stage review + full green CI: (1) `cargo deb` doesn't build the worker as a dependency, (2) postinst doesn't enable/start the service, (3) `ReasoningScheduler` has no logs, (4) top-level LLM calls bypass the new scheduler, (5) **hop-TTL ships non-functional — `decrement_hops` has zero callers**, (6) wall-clock TTL doesn't wrap scaffolded roles, (7) `SubtaskTtlExpired` not in the operator-visible whitelist. Lesson worth lifting: unit tests can be green while the feature ships broken if the test hard-codes the final state of a multi-step sequence instead of exercising the sequence itself.
- [`2026-04-18-f-b1-bugfix-reverify.md`](2026-04-18-f-b1-bugfix-reverify.md) — Re-verification on a fresh DO droplet of the 7 fixes from the prior day. Caught that the bug-fix sweep itself shipped two broken fixes: BUG #1's `pre-build-command` is not a cargo-deb field (TOML parse error on every build); BUG #2's `systemd-units = { enable=true, start=true }` is silently ignored when `maintainer-scripts` is also set. Real fixes shipped in `b24139b` (wrapper script) and `ce4582a` (inline systemd wiring in postinst). BUGs #5 + #7 verified end-to-end — operator sees `TTL expired (hops_exhausted): analyze` in the default CLI view. Lesson: "first on-production exercise" applies to bug-fix commits too, not just feature ships.
- [`2026-04-19-f-b2-e2e-qa.md`](2026-04-19-f-b2-e2e-qa.md) — Fresh-droplet e2e for Phase F-b sub-project 2 (dynamic model routing, 12 commits `cd55c8c`..`49ca6c6`). Passed first try: `./packaging/build-deb.sh` one-step build produces a working `.deb`; `apt install` auto-starts the daemon; `AAOS_DEFAULT_TASK_TTL_WALL_CLOCK_S=5` on the canonical HN+lobsters goal reliably triggers analyzer/writer tier-0 failures → operator sees `model escalated (replan_retry): analyze_comparison — deepseek-chat → deepseek-reasoner` in the default CLI stream. Three escalations emitted, one at ladder top correctly declined. No regressions, no panics. Lesson confirmation: the three process decisions lifted from sub-project 1 (operator-visibility as first-class task, fresh-droplet as Definition of Done, honest scope notes) prevent the re-verification regressions they're meant to prevent — no new pattern needed.
- [`2026-04-19-f-b3c-workspace-mount.md`](2026-04-19-f-b3c-workspace-mount.md) — Gap C closed. `PolicyDescription` gains `workspace: Option<PathBuf>` (bind-mounted at the same absolute path inside the worker, added to Landlock rw allow-list) + `extra_writable_roots: Vec<PathBuf>` derived from the manifest's `file_read`/`file_write` capability declarations. `SubtaskRunner` signature gains `run_root`; `run_subtask_inline` threads it to `backend.launch`. `AAOS_CONFINE_SUBTASKS` flips to **default-ON**. Droplet verification: full canonical HN+lobsters goal completes confined end-to-end in 152s — `/data/compare.md` = 6034 bytes, 5 `[worker]` + 4 `[daemon]` tags, zero tool failures, zero panics. Every DoD criterion hit first-try. Commits `47eb485` + `7adc147`. Pattern candidate: "extract structural truth from the manifest; bind-mount + Landlock + capability tokens are layered, not either/or."
- [`2026-04-19-f-b3b-gap-fix.md`](2026-04-19-f-b3b-gap-fix.md) — Closing the two Gap-3 QA findings. Gap B (capability re-check inside worker) fixed by forwarding `Vec<CapabilityToken>` across the broker — worker rebuilds per-call registry so tools' internal `ctx.capability_registry.permits(...)` succeeds. Gap A (inline subtask → worker session) fixed by calling `NamespacedBackend::launch` for each plan-executor subtask when `AAOS_CONFINE_SUBTASKS=1`; default off because today's worker mount namespace exposes only `/scratch` + shared libs, no workspace access. Droplet verification: default run completes canonical goal end-to-end (5.7 KB `compare.md` in 182s); `AAOS_CONFINE_SUBTASKS=1` on a `/proc/self/status` read goal shows **18 [worker] tags / 0 [daemon] tags**, workers launching cleanly, capability forwarding working, Landlock + capability-grant semantics taking effect. Also fixed `NamespacedBackendConfig::default()`'s session_dir from `/var/run/aaos/sessions` (root-only) to `/run/agentd/sessions` (agentd's RuntimeDirectory, aaos-writable). Shipped across commits `0716b1a`..`9c33e4d`. Workspace-bind-mount design is the next follow-up sub-project.
- [`2026-04-19-f-b3-e2e-qa.md`](2026-04-19-f-b3-e2e-qa.md) — Fresh-droplet e2e for Phase F-b sub-project 3 (worker-side tool confinement, 13 commits `0a47bb3`..`7a017f6`). Two bugs + one design-layer gap surfaced. (1) Every worker-routed call failed with `no broker session for agent <id>` — because plan-executor subtasks spawn inline via `execute_agent_for_subtask` and never go through `backend.launch`, so no session exists. Fixed by making `WorkerHandle::invoke_over_worker` return a structured `WorkerInvokeError { NoSession | Transport }` and having `ToolInvocation::invoke` fall back to daemon-side on `NoSession` with honest `execution_surface: Daemon` on the audit event (commit `7a017f6`). (2) Tool-layer capability re-check inside the worker fails because the worker's `InvocationContext` has an empty registry — tools encode their own `ctx.capability_registry.permits(...)` check that the T6 design assumed would be absent. Not fixed in v1; follow-up sub-project. Evidence the substrate works: `invoke_tool_landlock_denial` passes on the droplet — call crosses broker, enters worker, Landlock denies `/etc/shadow`. Gap honestly named in architecture.md: sub-project 3 confines only `spawn_agent`-launched agents today, not plan-executor subtasks. New pattern lifted: "test the path the canonical goal actually uses."
- [`2026-04-19-v0.0.1-droplet-qa.md`](2026-04-19-v0.0.1-droplet-qa.md) — QA of the shipped v0.0.1 `.deb` on a fresh Debian 13 droplet. Passed: install/start path, kernel-probe-driven `.example` generation, missing-key UX message, confinement end-to-end (once fixed, see Bug 1), corrupt-role YAML error path, MCP server loopback. Six bugs found: **Bug 1 (CRITICAL)** — `.deb` shipped without `--features namespaced-agents`, so `AAOS_DEFAULT_BACKEND=namespaced` silently fell back to `InProcessBackend`; fix landed in `packaging/build-deb.sh` default features. **Bug 2** zombie `aaos-agent-worker` children after successful runs (parent doesn't reap). **Bug 3** invalid API key → `bootstrap failed (0k in / 0k out, 0s)` with zero log context. **Bug 4** MCP client reconnect loop completely silent — no log lines for connect attempts, failures, or retries. **Bug 5** duplicate "using NamespacedBackend" startup log (two call sites). **Bug 6** 11 lintian errors (aliased `lib/systemd/`, Apache-2 copyright handling, unstripped binaries, missing changelog). **→ All six fixed and verified in v0.0.2 (`160861f` Bug 1, `d1cbe8c` Bugs 2–6). Final lintian: 0 errors + 0 warnings.**
- [`2026-04-19-v0.0.2-droplet-qa.md`](2026-04-19-v0.0.2-droplet-qa.md) — Verification re-run of the five-pass QA suite against the shipped v0.0.2 `.deb`. All six bugs verified closed end-to-end on the same droplet: 0 lintian findings, 0 zombies, `[worker]` tags on file ops, bad-key error legible in CLI + journald, MCP subsystem observable (both enabled and disabled states), single "using NamespacedBackend" per startup. Canonical goal: 27 s (v0.0.1: 57 s, already-stripped-binary and smaller log footprint shave off another ~30 %). Verdict: v0.0.2 delivers what v0.0.1 advertised; M1 (Debian-derivative image) is no longer blocked on a broken base package.
- [`2026-04-24-v0.0.3-self-reflection.md`](2026-04-24-v0.0.3-self-reflection.md) — First self-reflection run against a tagged release `.deb` under full confinement. Fresh droplet with v0.0.3 installed, aaOS pointed at its own source tree via a custom `reflector` role (single-agent, pre-scoped caps). Reflector read 15 production files in the security boundary in 45 s, then every `grep` call failed with `failed to spawn rg: Operation not permitted` — **Bug 8**: the `grep` tool lists as worker-side in `WORKER_SIDE_TOOLS` but shells out to `rg` via `execve`, which the worker's seccomp kill-filter denies. Same class as Bug 7 (routing-list drift). Fixed in `aaf82a3`, shipped as v0.0.4 the same day. Planner fallback-to-generalist also cascaded into a silent "NOT COMPLETED" report without surfacing the tool failure — queued as **Bug 9** under `[Unreleased]` pending a design choice on partial-success shape.
- [`2026-04-25-v0.1.0-first-real-findings.md`](2026-04-25-v0.1.0-first-real-findings.md) — First successful self-reflection run on the v0.1.0 substrate. Generalist subtask via `--orchestration persistent` ran 18 multi-turn iterations and produced 3 substantive bug findings with file:line references, reproduction sketches, and proposed fixes: `max_invocations` not enforced in tool-invocation hot path (High), worker per-call `CapabilityRegistry` defeats revocation + invocation_count (High), glob-prefix match incorrectly accepts `/data-foo/...` against pattern `/data/*` (Medium). The agent's `file_write` tool call was preempted by an unrelated stop-race (**Bug 13**) so the report didn't land on disk; preserved by extracting the tool-call content from journald. **Bug 14** (informational): commit-nudge plumbing is correct end-to-end but never exercised in this run (LLM never emitted EndTurn). All 3 findings need triage before fixes land in v0.1.1.
- [`2026-04-25-v0.1.2-bug-13-and-17.md`](2026-04-25-v0.1.2-bug-13-and-17.md) — Same-day continuation. v0.1.1 shipped 5 fixes (Bugs 10/11/12/15/16); Bug 13 diagnosed via a Sonnet sub-agent traced to tokio cancellation propagating through `.await` at `invoke_tool`, fixed via 500 ms drain window before all `exec_task.abort()` sites in `server.rs`.  Verified end-to-end on the droplet: a v0.1.0-source bug-hunt that previously lost its findings.md to the abort cancel succeeded — 10.9 KB report with 3 new candidate bug findings (TOCTOU in `narrow`, `clone3` seccomp allowlist gap, `BudgetTracker::maybe_reset` race) landed on disk at the workspace path.  But the same run surfaced **Bug 17** — `inline_direct_plan` hardcoded `workspace: "{run}/output.md"` so the file didn't land where the operator's goal text said ("/data/findings.md"). Same silent-misdelivery class as Bug 9 was. Fixed: omit the workspace param entirely, let the LLM follow the goal text.  Both fixes shipped as v0.1.2.  3 candidate findings (Bugs 18/19/20) queued in `[Unreleased]` for triage.

## Supporting

- [`cost-bookkeeping.md`](cost-bookkeeping.md) — dashboard vs token-math framing for all cost figures quoted in run entries.

## How to add a new entry

Create a new file `YYYY-MM-DD-<short-slug>.md` in this directory using the template below, then add a one-line summary to the list above.

```markdown
# Run N — <short name> *(YYYY-MM-DD)*

**Integration commits:** `<hash>` "<message>" (HH:MM), ...

## Setup
- Memory state: fresh / carried over from run N-1 / partial
- Philosophical / specific goal
- Notable config (AAOS_PERSISTENT_MEMORY, AAOS_RESET_MEMORY, etc.)

## What Worked
- ...

## What the Run Exposed
- ...

## What Shipped
- ...

## Cost
- Dashboard-authoritative figure if known, else note "[token-math estimate]"
```

New lessons that generalize across runs should be lifted into [`../patterns.md`](../patterns.md) rather than repeated in each entry.
